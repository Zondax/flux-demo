apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: storage
  namespace: ${namespace}
spec:
  interval: 10m
  sourceRef:
    kind: GitRepository
    name: flux-system
  serviceAccountName: ${serviceAccountKust}
  path: ./storage
  prune: true
  timeout: 1m
  postBuild:
    substitute:
      namespace: ${namespace}
      serviceAccountKust: ${serviceAccountKust}
